# 方法卡深化规则与进度（交接文档）

本文件是「把所有方法卡升级到 16 节深化标准」这项长期工作的**交接说明**，供新对话或其他 agent 接手继续。读完本文件即可独立接续，无需原对话上下文。

---

## 0. 一句话任务

把 `01_方法词典/` 下每张方法卡，从原来的 12 节结构，逐张全文改写为 **16 节深化标准**：更揭示方法本质、更适合初学者，且每张都带**能手算的数字算例**。按**分类逐类推进**，每轮约 4–8 张。

---

## 1. 16 节结构（唯一规范见 `02_模板/01-方法卡模板.md`）

1. 方法概览（1.1 一句话本质 / 1.2 定义 / 1.3 解决什么问题 / 1.4 直觉与类比）
2. **核心思想与原理**（2.1 到底解决什么根本困难 / 2.2 关键洞察 / 2.3 与朴素做法对比）
3. 数学形式（3.1 核心公式 / 3.2 推导脉络 / 3.3 参数含义 / 3.4 关键假设含违反后果表）
4. **手把手算例**（带真实数字、能一步步手推）
5. 数据形式与输入输出（5.1 适合形式 / 5.2 示例表格 / 5.3 输入产出）
6. 适用场景
7. 实现（7.1 Python / 7.2 R，代码分栏靠这两个 `### 7.x` 子标题识别）
8. 结果如何解读
9. **假设诊断与稳健性**
10. 推荐可视化（原卡的配图放这里，见下方硬约定）
11. 优势、局限与常见坑
12. 与相近方法的区别（含"如何选择"一句决策经验）
13. 医学研究中的典型应用
14. **关键术语**（初学者中英对照小词典）
15. 相关方法（双链）
16. 参考资料

加粗的 2/4/9/14 节是相对旧 12 节**新增**的四节，是"去空洞"的核心。

---

## 2. 深化写作规则（决定一张卡"深不深"）

- **揭示本质 > 罗列步骤**：§2 必须讲清「不用它会撞上什么根本困难 + 用什么关键洞察绕过」。这是最重要的一节。
- **先直觉后公式**：每节先给白话/类比，再上公式；公式后紧跟一句「这个式子在说……」。
- **手把手算例（§4）不可省**：3–8 个观测，逐步代入公式，展示中间量，最后从数字读出结论并翻译成人话。范例见已完成卡：
  - 相关三卡（Pearson/Spearman/Kendall）**共用同一份 5 点数据**得 r=0.90 / ρ=0.90 / τ=0.80，直观对照。
  - ANOVA 卡手算 F=12，TukeyHSD 卡**沿用同一数据**接着算 HSD=2.51 —— 例子前后咬合是加分项。
  - Poisson 手算 IRR=2.0=exp(0.69)；负二项由方差 20/均值 5 解出 α=0.6；Quasi 手算 φ=3、SE×√3 后结论翻转。
- **难以手算的方法**（如 RCS 的样条基）：§4 改走"工作流"式算例（选结点→数自由度→非线性检验→读效应曲线），并诚实说明为何不逐项手推。
- **§14 术语表**：把本卡出现的关键术语各用一句话中英对照解释。
- 医学语境：标注结局类型、缺失机制、删失、混杂、类别不平衡等。

---

## 3. 硬性技术约定（违反会破坏站点或检查）

1. **frontmatter 的 `slug` 与 `aliases` 一字不改**：改写时必须原样保留（slug 是网站 URL、双链靠 aliases 全名解析）。改写前先 `Read` 原卡取这两项。
2. **保留原卡的配图引用**：否则 `check.py` 报「孤儿图，未被任何方法卡引用」错误。把原来的 `![](../../04_示例图像/xxx.png)` 迁到 **§10 推荐可视化**（可加一个 `### 10.1 图像示例` 小节）。改写前先 grep 原卡有无配图。
3. **LaTeX 规范**（`build_site.py` 会转义，但仍应写干净）：
   - **不要用裸 `<` `>`**（会被浏览器当 HTML 标签破坏 DOM）；用 `\lt \gt \le \ge` 或中文"小于/大于"。
   - 花括号必须配平；`\frac` 分母要带 `{}`。
   - KaTeX 支持 `\binom \dbinom \begin{aligned} \begin{smallmatrix}` 等；`\begin{align}`（无 aligned）、`\bm`、`\argmin` 不支持。
4. **代码分栏**：§7 实现必须有 `### 7.1 Python` 和 `### 7.2 R` 两个子标题（`build_site.py` 靠"标题含'实现' + 存在 `### x.y`"来渲染 Python/R 分栏）。
5. **双链写全名** `[[中文名（英文名）]]`；相关的未建卡也可链（允许，显示为提示）。
6. **不改文件名/编号/目录**；一次只改正文，frontmatter 只在必要时（如 status）动。

---

## 4. 每张卡的工作流

1. `Read` 原卡（`limit: 1` 即可登记，但要先用 grep/sed 抓 `slug`、`aliases`、配图路径）。
2. `Write` 全文改写为 16 节（保留 slug/aliases/配图）。
3. 一批（4–8 张）写完后：`python3 scripts/check.py` → 必须 **0 错误 0 提示**。
4. `python3 scripts/build_site.py` 重新生成 `docs/`。
5. **KaTeX 审计**（强烈建议）：用 Preview 打开任一详情页加载 KaTeX，再 fetch 本批各页、剥掉 `<pre><code>`、对每个 `$$…$$`/`$…$` 跑 `katex.renderToString({throwOnError:true})`，确认 0 失败、各页 16 个 TOC 项、2 个代码分栏。（本会话用的审计 JS 片段可复用；核心是"逐条公式送进 KaTeX 验证"。）
6. `git add -A && git commit`（提交信息说明本批改了哪些、手算了什么）。
7. 推送（见 §5）。
8. 验证部署成功（见 §5）。

---

## 5. 校验、推送与部署命令

```bash
# 一致性检查（0 错误 0 提示才算过）
python3 scripts/check.py

# 重新生成站点
python3 scripts/build_site.py

# 推送：本机环境把 github.com:22 拦截重定向,必须走 SSH-over-443
GIT_SSH_COMMAND='ssh -p 443 -o Hostname=ssh.github.com -o StrictHostKeyChecking=accept-new' \
  git push origin main

# GitHub Pages 偶发 "Deployment failed, try again later"（GitHub 侧瞬时故障,非本仓库问题）:
gh run list --workflow "pages-build-deployment" -L 3      # 看状态
gh run rerun <失败的 run_id>                              # 重跑;最新 commit 最终胜出
```

- 远程是 SSH：`git@github.com:fxt-gw-pb/BlackBoxDictionary.git`。
- 线上站点：`https://fxt-gw-pb.github.io/BlackBoxDictionary/`（GitHub Pages，source = main 分支 /docs 目录）。
- 快速验证某张卡是否已上线且深化：`curl -s <url>/m-<slug>.html | grep -c '手把手算例'`。

---

## 6. 进度快照（截至最近一次提交）

**已深化 37 / 140。** 按"分类逐类推进"，已完成 01、02、03 三个分类：

| 分类 | 已深化/总 | 状态 |
| --- | --- | --- |
| 01 研究设计与数据理解 | 5 / 5 | ✅ |
| 02 描述统计与统计推断 | 20 / 20 | ✅ |
| 03 回归与广义线性模型 | 12 / 12 | ✅ |
| 04 生存分析与纵向数据 | 0 / 10 | ⬜ 下一类 |
| 05 因果推断 | 0 / 4 | ⬜ |
| 06 正则化与变量选择 | 0 / 3 | ⬜ |
| 07 树模型与集成学习 | 0 / 16 | ⬜ |
| 08 支持向量机与核方法 | 0 / 4 | ⬜ |
| 09 聚类与无监督学习 | 0 / 10 | ⬜ |
| 10 降维与表征学习 | 0 / 10 | ⬜ |
| 11 神经网络与深度学习 | 0 / 13 | ⬜ |
| 12 时间序列与时序建模 | 0 / 10 | ⬜ |
| 13 数据预处理与特征工程 | 0 / 8 | ⬜ |
| 14 高维数据与多组学 | 0 / 0 | 空目录（尚无卡，可另议是否新增） |
| 15 模型评估与解释 | 0 / 10 | ⬜ |
| 16 分类与判别方法 | 0 / 5 | ⬜ |

**随时重新生成进度快照**（判定标准：正文有 16 个 `## N.` 主标题即视为已深化）：

```bash
cd 01_方法词典
for d in */; do t=0; k=0; for f in "$d"*.md; do [ -e "$f" ] || continue; t=$((t+1)); \
  [ "$(grep -c '^## [0-9]' "$f")" = 16 ] && k=$((k+1)); done; printf "%-30s %d/%d\n" "${d%/}" "$k" "$t"; done
```

---

## 7. 建议的下一步

- **下一类：04 生存分析与纵向数据（10 张）** —— 医学统计重头戏：Kaplan-Meier、Nelson-Aalen、Log-rank、Cox（注：Cox 已在样板阶段深化过，接手时先用上面的命令确认它是否已是 16 节，避免重复）、AFT、竞争风险/Fine-Gray、线性混合效应、GLMM、GEE、条件 Logistic。建议分两轮。
- 之后按分类序号 05 → 16 逐类推进。
- 每类做完在本文件 §6 表格里更新状态。

---

## 8. 已完成卡可作范例

改写风格与深度直接参照这些已上线的 16 节卡：

- 回归主线：`线性回归`、`广义线性模型`、`Logistic回归`、`Poisson回归`、`负二项回归`、`Quasi-Likelihood与过度离散`
- 检验主线：`两独立样本t检验`、`单因素方差分析` + `TukeyHSD多重比较`（例子咬合）
- 相关三卡：`Pearson相关`/`Spearman秩相关`/`Kendall秩相关`（共用数据对照）
- 分布/设计：`样本量与检验效能`、`直方图`、`ECDF`、`KDE`、`Kolmogorov-Smirnov检验`
